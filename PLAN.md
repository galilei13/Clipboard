# پلن ساخت Clipboard برای macOS

وضعیت: نسخهٔ آزمایشی 0.1.0 ساخته و روی مک کاربر اجرا شده و ۲۰ بررسی خودکار موفق بوده‌اند. برنامه آمادهٔ آزمایش روزمره و رفع باگ در چرخهٔ ۰.x است. جزئیات و موارد نیازمند بررسی دستی در README.md و VALIDATION.md آمده‌اند؛ رسیدن به نسخهٔ ۱ و انتشار GitHub همچنان مرحله‌ای بعدی است.

مسیر نسخه‌ها: توسعه و رفع باگ در نسخه‌های ۰.x؛ رسیدن به نسخهٔ پایدار ۱.۰ و سپس انتشار روی GitHub. انتخاب لایسنس هنوز باز است و با تصمیم مشترک انجام می‌شود.

## هدف و محدوده

یک اپ بومی و سبک برای منوبار مک با رابط انگلیسی، تاریخچهٔ ۲۴ ساعته، پین زمان‌دار و استفادهٔ سریع با کیبورد. محتوای فارسی، متن ترکیبی و ایموجی باید درست نمایش داده شوند.

مبنای طراحی، پیش‌نمایش ارائه‌شده در همین گفتگو است: پنل حدود ۴۲۰ پیکسل، جست‌وجو در بالا، دو بخش Recent و Pinned، ردیف‌های خوانا و رنگ آبی ملایم. اندازهٔ نهایی پنل متناسب با فضای نمایشگر تنظیم می‌شود و تاریخچهٔ طولانی اسکرول دارد.

چهار قابلیت اضافهٔ تأییدشده:

1. Paste بدون فرمت.
2. ادغام موارد تکراری.
3. کنترل کامل با کیبورد.
4. پشتیبانی از متن، لینک و تصویر.

توقف موقت ثبت، فهرست برنامه‌های مستثنا و نام‌گذاری پین‌ها در این نسخه وجود ندارند. OCR، همگام‌سازی ابری، پوشه‌بندی و چسباندن چند مورد پشت سر هم نیز در محدودهٔ این نسخه نیستند.

## رفتار اصلی برنامه

- برنامه در منوبار اجرا شود و پنجرهٔ دائمی یا آیکن Dock نداشته باشد.
- کلیک روی آیکن یا میان‌بر پیش‌فرض ⌘⇧V پنل را باز و بسته کند.
- هنگام بازشدن، جست‌وجو آمادهٔ تایپ باشد.
- کلیک بیرون پنل یا Escape آن را ببندد.
- از زمان اجرای برنامه، تغییرات کلیپ‌بورد شامل متن، لینک و تصویر از همهٔ برنامه‌ها ثبت شوند؛ کنترل توقف و فیلتر بر اساس برنامهٔ مبدأ نداشته باشیم.
- محتوا و تنظیمات روی همان مک ذخیره شوند و پس از بستن و بازکردن برنامه باقی بمانند.
- محتوای تاریخچه به شبکه ارسال نشود.
- Quit در تنظیمات یا منوی برنامه در دسترس باشد.

## تاریخچه، انقضا و موارد تکراری

- Recent آیتم‌های بدون پین را، از جدیدترین زمان کپی به قدیمی‌ترین، نشان دهد.
- هر آیتم معمولی تا ۲۴ ساعت پس از آخرین کپی نگهداری شود.
- کپی مجدد محتوای یکسان، همان آیتم را به‌روز کند و به بالای فهرست ببرد؛ ردیف تکراری نسازد.
- ادغام نباید فاصله‌ها، شکست خطوط، ایموجی یا محتوای اصلی متن را تغییر بدهد. لینک‌ها بر اساس محتوای واقعی کپی‌شده مقایسه شوند.
- برای یک تصویر، نسخه‌های مختلف ارائه‌شده در همان عملیات کپی به یک آیتم تبدیل شوند. تشخیص تصاویر تکراری بر اساس محتوای تصویر انجام شود.
- هر آیتم مجموعهٔ قالب‌های پشتیبانی‌شدهٔ همان کپی را نگه دارد تا Paste معمولی و Paste بدون فرمت ممکن باشند.
- استفادهٔ مجدد از آیتم داخل Clipboard فقط همان آیتم را به‌روز کند؛ نوشتن برنامه در کلیپ‌بورد باعث ثبت حلقه‌ای یا ردیف تکراری نشود.
- انقضا هنگام اجرای برنامه، بازکردن پنل، بازگشت از Sleep و در حین اجرا بررسی شود. موارد منقضی‌شده پیش از نمایش از نتایج حذف شوند.
- اطلاعات و تصاویر منقضی‌شده از ذخیره‌سازی برنامه پاک شوند. پاک‌سازی تاریخچه فقط داده‌های خود برنامه را تغییر بدهد؛ محتوای جاری کلیپ‌بورد سیستم یا فایل‌های اصلی کاربر پاک نشوند.

## پین

- آیکن پین هر ردیف، منوی چهارگزینه‌ای باز کند: 3 days، 1 week، 1 month و Forever.
- مدت پین از زمان انتخاب گزینه محاسبه شود: سه روز، هفت روز، یک ماه تقویمی یا بدون انقضا.
- Pinned آیتم‌های دارای پین فعال را نشان دهد؛ هر آیتم در یک بخش نمایش داده شود.
- کنار آیتم، زمان باقی‌مانده یا Pinned forever نمایش داده شود.
- امکان تغییر مدت و Unpin از همان منو وجود داشته باشد. انتخاب مدت جدید، آن را از همان لحظه محاسبه کند.
- کپی مجدد آیتم پین‌شده، زمان آخرین کپی را تازه کند؛ مدت پین به‌صورت خودکار تمدید نشود.
- وقتی پین تمام یا برداشته شد، اگر آخرین کپی هنوز در بازهٔ ۲۴ ساعت است، آیتم به Recent برگردد؛ در غیر این صورت از تاریخچه و ذخیره‌سازی حذف شود.
- مثال: آیتمی با پین سه‌روزه که دیگر کپی نشده، پس از پایان سه روز حذف می‌شود. اگر دو ساعت قبل از پایان پین دوباره کپی شده باشد، ۲۲ ساعت دیگر در Recent می‌ماند.

## محتوا، جست‌وجو و پیش‌نمایش

- متن: نمایش یک خلاصهٔ خوانا در ردیف و متن کامل در پیش‌نمایش.
- لینک: نمایش نشانی یا دامنه به‌صورت قابل‌تشخیص؛ انتخاب آیتم برای Copy/Paste باشد و لینک خودکار باز نشود.
- تصویر: نمایش بندانگشتی و امکان پیش‌نمایش بزرگ‌تر؛ فایل تصویر برای Paste حفظ شود.
- آیکن نوع محتوا، زمان کپی و نام برنامهٔ مبدأ در صورت قابل‌تشخیص‌بودن نشان داده شوند.
- جست‌وجو در بخش انتخاب‌شده روی متن و لینک‌ها انجام شود و محتوای فارسی را هم پوشش بدهد. تصاویر در این نسخه جست‌وجو بر اساس متن داخل تصویر ندارند.
- حالت تاریخچهٔ خالی، بخش Pinned خالی و جست‌وجوی بدون نتیجه، پیام کوتاه و روشن داشته باشند.
- در نسخهٔ نهایی دکمهٔ توقف ثبت از طرح اولیه حذف شود.

## Copy، Paste و کیبورد

| عمل | رفتار پیشنهادی برای پیاده‌سازی |
| --- | --- |
| ⌘⇧V | باز و بسته‌کردن پنل؛ قابل تغییر در Settings |
| ↑ / ↓ | جابه‌جایی بین نتایج قابل‌مشاهده |
| Enter | Paste آیتم انتخاب‌شده در برنامه‌ای که پیش از بازشدن پنل فعال بوده |
| ⇧Enter | Paste متن یا لینک بدون فرمت |
| ⌘C | کپی آیتم انتخاب‌شده در کلیپ‌بورد برای استفادهٔ دستی |
| ⌘1 تا ⌘9 | Paste مستقیم یکی از ۹ نتیجهٔ اول بخش/جست‌وجوی فعلی |
| Space | باز و بسته‌کردن پیش‌نمایش وقتی تمرکز روی لیست است |
| ⌘F | انتقال تمرکز به جست‌وجو |
| Escape | بستن منوی پین یا پیش‌نمایش؛ سپس بستن پنل |

- میان‌برهای لیست در هنگام ویرایش متن جست‌وجو، عملیات عادی تایپ، انتخاب متن و کپی را مختل نکنند.
- کلیک روی ردیف آن را انتخاب کند؛ دکمه‌های Copy و Paste و منوی پین با ماوس نیز در دسترس باشند.
- Paste معمولی قالب‌های قابل‌استفادهٔ محتوای ذخیره‌شده را حفظ کند؛ Paste بدون فرمت فقط متن ساده بنویسد و برای تصویر قابل انتخاب نباشد.
- پیش از Paste، پنل بسته شود و تمرکز به برنامهٔ قبلی برگردد.
- جریان Paste مستقیم شامل بررسی مجوز Accessibility و راهنمای فعال‌کردن آن باشد. در نبود مجوز، گزینهٔ Copy برای استفادهٔ دستی در دسترس بماند و برنامه ادعای Paste موفق نکند.
- میان‌بر سراسری جدید اعتبارسنجی شود. در صورت تداخل یا ثبت‌نشدن، پیام روشن نمایش داده شود و میان‌بر معتبر قبلی حفظ شود.

## تنظیمات

- Keyboard shortcut: ضبط و تغییر میان‌بر بازکردن پنل.
- Open at login: کلید روشن/خاموش با وضعیت واقعی ثبت‌شده در macOS؛ پیش‌فرض خاموش تا کاربر فعالش کند.
- Appearance: System، Light و Dark؛ پیش‌فرض System.
- تاریخچهٔ معمولی: مدت ثابت ۲۴ ساعت، با توضیح جداگانه برای پین‌ها.
- Clear recent history: پاک‌کردن موارد بدون پین با تأیید داخل برنامه.
- Quit: خروج از برنامه.
- تمام تنظیمات بعد از اجرای دوباره حفظ شوند.

## مسیر فنی

- اپ بومی با SwiftUI برای رابط و AppKit برای منوبار، پنل، مدیریت تمرکز و کلیپ‌بورد.
- استفاده از Swift Package و اسکریپت ساخت بستهٔ .app؛ ساخت اولیه با ابزارهای Swift موجود روی مک کاربر امکان‌پذیر باشد.
- منطق تاریخچه، انقضا، پین و ادغام از رابط جدا باشد تا با ساعت قابل‌کنترل آزمایش شود.
- ذخیره‌سازی محلی با پایگاه دادهٔ SQLite و فایل‌های مدیریت‌شده برای دادهٔ تصویر؛ حذف آیتم، داده‌های وابستهٔ آن را هم پاک کند.
- تولید بندانگشتی، پردازش تصویر و ذخیره‌سازی سنگین، رابط را متوقف نکنند؛ فهرست به‌صورت تدریجی رندر شود.
- Open at login با سازوکار رسمی Service Management پیاده شود و وضعیت رد یا نیازمند تأیید نیز در تنظیمات درست منعکس شود.
- اعتبارسنجی اجرایی اولیه روی مک فعلی کاربر، Apple Silicon با macOS 26.5.2، انجام شود.

## مراحل ساخت و خروجی هر مرحله

1. **پوستهٔ قابل اجرا:** ساختار پروژه، بستهٔ .app، آیکن منوبار، پنل، ظاهر روشن/تاریک و بازشدن با میان‌بر پیش‌فرض.
2. **تاریخچهٔ واقعی:** خواندن متن/لینک/تصویر، ذخیره‌سازی، ادغام تکراری‌ها، پاک‌سازی ۲۴ ساعته و بازیابی بعد از اجرای دوباره.
3. **پین و مرور:** مدت‌های چهارگانه، تمدید و Unpin، بخش‌های Recent/Pinned، جست‌وجو و پیش‌نمایش متن و تصویر.
4. **استفادهٔ سریع:** Copy، Paste، Paste بدون فرمت، مدیریت تمرکز، کنترل کیبورد و رسیدگی به وضعیت مجوز Paste.
5. **تنظیمات و تحویل آزمایشی:** تغییر میان‌بر، Open at login، پاک‌سازی تاریخچه، بررسی اولیه، بستهٔ .app با شمارهٔ نسخهٔ ۰.x و راهنمای اجرای کوتاه.
6. **تثبیت نسخه‌های پیش از ۱:** استفادهٔ واقعی، ثبت باگ‌ها، رفع مشکلات و تکرار بررسی‌ها در نسخه‌های بعدی ۰.x تا آماده‌شدن نسخهٔ پایدار.
7. **انتشار نسخهٔ ۱:** نهایی‌کردن لایسنس با کاربر، رسیدن به معیارهای نسخهٔ پایدار و انتشار پروژه و بستهٔ نسخهٔ ۱.۰ روی GitHub.

## نسخه‌بندی و انتشار روی GitHub

- اولین خروجی قابل استفاده یک نسخهٔ پیش از ۱ باشد؛ شماره‌گذاری از 0.1.0 شروع شود و در چرخه‌های توسعه و رفع باگ در خانوادهٔ 0.x ادامه پیدا کند.
- کامل‌شدن امکانات به‌تنهایی به معنی رسیدن به نسخهٔ ۱ نیست. پیش از آن، برنامه در استفادهٔ واقعی آزمایش شود، باگ‌های گزارش‌شده بررسی شوند و مشکلات مانع استفادهٔ پایدار برطرف شوند.
- تغییرات و باگ‌های اصلاح‌شدهٔ هر نسخه در CHANGELOG ثبت شوند؛ شمارهٔ نسخه در برنامه و بستهٔ خروجی با هم هماهنگ باشد.
- رسیدن به 1.0.0 پس از گذراندن معیارهای پذیرش، رفع باگ‌های مهم شناخته‌شده و جمع‌بندی مشترک با کاربر دربارهٔ آمادگی انتشار انجام شود.
- انتشار روی GitHub پس از رسیدن به نسخهٔ ۱ انجام شود. مرحلهٔ فعلی توسعه و آزمایش محلی نسخه‌های ۰.x است و شامل انتشار روی GitHub نمی‌شود.
- خروجی انتشار شامل مخزن پروژه و یک GitHub Release با برچسب v1.0.0، بستهٔ قابل دانلود برنامه، یادداشت‌های انتشار و راهنمای نصب و استفاده باشد.
- نام مخزن و حساب یا سازمان مقصد GitHub پیش از مرحلهٔ انتشار مشخص شوند.
- پیش از انتشار عمومی، روش بسته‌بندی، امضای برنامه و نیاز به notarization برای نصب روی مک‌های دیگر بررسی و دربارهٔ روش تحویل تصمیم گرفته شود؛ نتیجهٔ بررسی نصب همراه انتشار مستند شود.

## تصمیم ثبت‌شده: آپدیت برنامه در مرحلهٔ انتشار ۱.۰

- طبق تصمیم کاربر در ۹ سپتامبر ۲۰۲۶، راه‌اندازی آپدیت آنلاین تا مرحلهٔ نهایی آماده‌سازی انتشار رسمی 1.0.0 به تعویق بیفتد؛ در چرخهٔ فعلی توسعه و تست 0.x انجام نشود.
- روش مورد توافق: GitHub Releases برای میزبانی بسته‌های انتشار، همراه Sparkle برای بررسی، دانلود، اعتبارسنجی و نصب آپدیت از داخل برنامه.
- پیش از انتشار بستهٔ رسمی ۱.۰، Sparkle به برنامه متصل شود، آدرس فهرست نسخه‌ها (appcast) مشخص و فرایند تولید و امضای آپدیت‌ها آماده شود؛ دکمهٔ Check for Updates به این مسیر وصل و چرخهٔ آپدیت آزمایش شود.
- تا آن مرحله، پیام فعلیِ در دسترس نبودن آپدیت آنلاین حفظ شود؛ برنامه بدون بررسی واقعی اعلام نکند که به‌روز است.
- این تصمیم، برنامهٔ کار آینده است و به معنی انتشار یا راه‌اندازی آپدیت در حال حاضر نیست.

## لایسنس — تصمیم باز

- نوع لایسنس برنامه هنوز انتخاب نشده و بعداً با کاربر دربارهٔ آن تصمیم گرفته می‌شود.
- تا پیش از این تصمیم، لایسنس مشخصی به پروژه نسبت داده نشود و فایل LICENSE با انتخاب پیش‌فرض ساخته نشود.
- انتخاب لایسنس و مشخصات صاحب اثر، پیش از انتشار نسخهٔ ۱ روی GitHub نهایی شوند؛ سپس فایل LICENSE و توضیح متناظر در README اضافه شوند.

## معیارهای پذیرش

- کلیپ‌بورد واقعی از چند برنامه، شامل متن فارسی و انگلیسی، ایموجی، لینک، متن قالب‌دار و تصویر ثبت و دوباره قابل استفاده باشد.
- ظاهر و تعامل‌ها در Light، Dark و System خوانا و یکدست باشند؛ پنل روی نمایشگر فعال خارج از صفحه قرار نگیرد.
- کپی تکراری تعداد ردیف‌ها را زیاد نکند و زمان آخرین کپی را به‌روز کند.
- مرز دقیق ۲۴ ساعت، پایان هر مدت پین، Forever، تمدید، Unpin و کپی مجدد آیتم پین‌شده با ساعت آزمایشی بررسی شوند؛ نیاز به انتظار واقعی چندروزه نباشد.
- داده‌های غیرمنقضی پس از اجرای دوباره باقی بمانند و داده‌های منقضی پس از بازگشت از Sleep یا اجرای مجدد نمایش داده نشوند.
- Paste به برنامهٔ قبلی برگردد؛ حالت بدون مجوز و لغو عملیات نیز بررسی شود. متن عادی و بدون فرمت و تصویر، هرکدام در مقصد مناسب آزمایش شوند.
- میان‌بر پیش‌فرض و میان‌بر تغییرکرده کار کنند و خطای تداخل باعث از دست رفتن میان‌بر قبلی نشود.
- وضعیت Open at login پس از فعال‌سازی و غیرفعال‌سازی با تنظیمات سیستم هماهنگ باشد؛ ورود واقعی به حساب فقط در صورت انجام آن توسط کاربر تأییدشده گزارش شود.
- تاریخچهٔ حجیم و تصاویر بزرگ باعث توقف رابط یا حذف بی‌اطلاع آیتم‌های معتبر نشوند.
- توقف ثبت، استثنای برنامه‌ها و نام‌گذاری پین در رابط نهایی وجود نداشته باشند.

## تحویل نهایی

- کد منبع و فایل پلن در همین پوشهٔ پروژه.
- یک فایل Clipboard.app قابل اجرای محلی روی مک کاربر.
- دستور ساخت مجدد و راهنمای کوتاه مجوزها، میان‌برها و تنظیمات.
- گزارش روشن از بررسی‌های انجام‌شده و مواردی که به تعامل مستقیم با macOS نیاز داشته‌اند.
- در دورهٔ آزمایشی: بسته‌های محلی ۰.x به همراه فهرست تغییرات و مشکلات شناخته‌شده.
- پس از رسیدن به نسخهٔ پایدار: مخزن GitHub و Release نسخهٔ v1.0.0، بستهٔ دانلود برنامه، CHANGELOG، راهنمای نصب و لایسنس مورد توافق.

## منابع فنی

- [Apple: mainAppService برای اجرای برنامه هنگام ورود](https://developer.apple.com/documentation/servicemanagement/smappservice/mainapp)
- [Apple: ثبت سرویس و وضعیت تأیید کاربر](https://developer.apple.com/documentation/servicemanagement/smappservice/register%28%29)
- [Apple: بررسی دسترسی Accessibility](https://developer.apple.com/documentation/applicationservices/1459186-axisprocesstrustedwithoptions)
